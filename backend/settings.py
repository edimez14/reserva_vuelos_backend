"""
Resumen:
Este archivo es el "cerebro de configuración" del backend.
Aquí se define conexión a base de datos, apps instaladas, seguridad, JWT, CORS y correo.

Django settings for backend project.

Generated by 'django-admin startproject' using Django 5.1.6.

For more information on this file, see
https://docs.djangoproject.com/en/5.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.1/ref/settings/
"""

import os
from pathlib import Path
from dotenv import load_dotenv
from urllib.parse import urlparse, parse_qsl
from datetime import timedelta

from decouple import config

load_dotenv()


def _normalize_origin(origin: str) -> str:
    origin = origin.strip()
    if not origin:
        return ''
    parsed = urlparse(origin)
    if parsed.scheme and parsed.netloc:
        return f'{parsed.scheme}://{parsed.netloc}'
    return origin.rstrip('/')

# Nota junior: BASE_DIR nos ayuda a construir rutas absolutas sin escribir rutas largas a mano.
# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.getenv('SECRET_KEY')

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.getenv('DEBUG') == 'True'

# ALLOWED_HOSTS: en Fly.io el dominio es <app>.fly.dev
# Se pueden añadir hosts adicionales mediante la variable de entorno ALLOWED_HOSTS.
_allowed_env = os.getenv('ALLOWED_HOSTS', '')
ALLOWED_HOSTS = [h.strip() for h in _allowed_env.split(',') if h.strip()]
if not ALLOWED_HOSTS:
    ALLOWED_HOSTS = []
# Permite health checks internos de Fly.io (necesario para el release command).
ALLOWED_HOSTS += ['localhost', '127.0.0.1', '.fly.dev']

# API key para AviationStack (servicio externo de vuelos).
# Estas variables permiten que la app consulte vuelos reales.
AVIATIONSTACK_API_KEY = config('AVIATIONSTACK_API_KEY', default='')
AVIATIONSTACK_API_URL = config('AVIATIONSTACK_API_URL', default='http://api.aviationstack.com/v1/flights')
AVIATIONSTACK_DEFAULT_LIMIT = config('AVIATIONSTACK_DEFAULT_LIMIT', default=9, cast=int)
FLIGHTS_CACHE_TIMEOUT = config('FLIGHTS_CACHE_TIMEOUT', default=900, cast=int)
HOME_FLIGHTS_AIRPORTS = config('HOME_FLIGHTS_AIRPORTS', default='BOG,MDE,CTG').split(',')

# Application definition
# Aquí registramos las apps que Django va a cargar.
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Third party
    'rest_framework',
    'rest_framework_simplejwt',
    'corsheaders',
    # Local apps
    'apps.users',
    'apps.flights',
    'apps.reservations',
    'apps.tickets',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    # WhiteNoise debe ir justo después de SecurityMiddleware para servir archivos
    # estáticos de forma eficiente en producción sin necesidad de Nginx.
    'whitenoise.middleware.WhiteNoiseMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    # Middleware propio para controlar expiración durante compra.
    'backend.middleware.PurchaseTimeoutMiddleware',
]

ROOT_URLCONF = 'backend.urls'

# Sin esto, Django intenta redirigir POST /reservations → /reservations/ y explota.
# Con False, Django no agrega la barra final automáticamente.
APPEND_SLASH = False

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'backend.wsgi.application'

# Database
# Leemos DATABASE_URL y la convertimos a los campos que Django entiende.
tmpPostgres = urlparse(os.getenv("DATABASE_URL", "postgresql://localhost:5432/postgres"))
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': tmpPostgres.path.replace('/', ''),
        'USER': tmpPostgres.username,
        'PASSWORD': tmpPostgres.password,
        'HOST': tmpPostgres.hostname,
        'PORT': tmpPostgres.port or 5432,
        'OPTIONS': dict(parse_qsl(tmpPostgres.query)),
    }
}

# Password validation
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator'},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
]

# Internationalization
LANGUAGE_CODE = 'es-co'
TIME_ZONE = 'America/Bogota'
USE_I18N = True
USE_TZ = True

# Static files (CSS, JavaScript, Images)
STATIC_URL = 'static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'
# WhiteNoise: compresión y cache-busting automático para archivos estáticos.
STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

# Default primary key field type
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',
        'LOCATION': 'reserva-vuelos-cache',
    }
}

# Custom user model
AUTH_USER_MODEL = 'users.User'

# REST Framework
# Config global para APIs: autenticación por JWT y permisos por defecto.
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.IsAuthenticated',
    ),
}

# JWT Settings
# Define cuánto duran los tokens y cómo se firman.
SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(minutes=60),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=1),
    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
    'UPDATE_LAST_LOGIN': True,
    'ALGORITHM': 'HS256',
    'SIGNING_KEY': SECRET_KEY,
    'VERIFYING_KEY': None,
    'AUTH_HEADER_TYPES': ('Bearer',),
    'USER_ID_FIELD': 'id',
    'USER_ID_CLAIM': 'user_id',
    'AUTH_TOKEN_CLASSES': ('rest_framework_simplejwt.tokens.AccessToken',),
    'TOKEN_TYPE_CLAIM': 'token_type',
}

# CORS
# Permite que el frontend (otro dominio/puerto) hable con este backend.
_cors_env = os.getenv('CORS_ALLOWED_ORIGINS', '')
CORS_ALLOWED_ORIGINS = [
    normalized
    for origin in _cors_env.split(',')
    for normalized in [_normalize_origin(origin)]
    if normalized
]
CORS_ALLOW_CREDENTIALS = True

# Email configuration
# Datos para envío de correos (registro, recuperación y confirmaciones).
EMAIL_BACKEND = os.getenv('EMAIL_BACKEND')
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = config('EMAIL_HOST', default='smtp.gmail.com')
EMAIL_PORT = config('EMAIL_PORT', default=587, cast=int)
EMAIL_USE_TLS = config('EMAIL_USE_TLS', default=True, cast=bool)
EMAIL_HOST_USER = config('EMAIL_HOST_USER', default='')
EMAIL_HOST_PASSWORD = config('EMAIL_HOST_PASSWORD', default='')
DEFAULT_FROM_EMAIL = os.getenv('DEFAULT_FROM_EMAIL', 'no-reply@example.com')

# External flight API
# FLIGHT_API_KEY = os.getenv('FLIGHT_API_KEY')
# FLIGHT_API_URL = os.getenv('FLIGHT_API_URL', 'https://api.example.com')