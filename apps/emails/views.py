from django.shortcuts import render

# Resumen:
# Esta app por ahora no expone endpoints propios.
# Se usa principalmente desde `services.py` para enviar correos desde otras apps.
# Create your views here.
